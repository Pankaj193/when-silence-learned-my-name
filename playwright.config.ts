import { createDeathConfig } from "lovable-agent-playwright-config/config";

export default createDeathConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
